apiVersion: v1
kind: Service
metadata:
  name: {{ include "titanos.fullname" . }}
  labels:
    {{- include "titanos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "titanos.selectorLabels" . | nindent 4 }}
